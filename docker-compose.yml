version: "2"
services:
  steaphapi:
    build: .
    ports:
      - "3000:3000"
    links:
      - mongo
      - mqtt
  mongo:
    image: mongo
    volumes:
      - /data/db:/data/db
    ports:
      - "27017:27017"